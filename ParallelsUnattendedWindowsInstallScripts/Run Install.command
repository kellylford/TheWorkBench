#!/usr/bin/env bash
# Double-click this file in Finder to install Windows 11 ARM.
# macOS will open it in Terminal automatically.

# Change to the folder containing this script
cd "$(dirname "$0")"

bash install-windows.sh

echo ""
echo "Press Return to close this window."
read -r
