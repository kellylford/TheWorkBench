# The Card Place for iOS

Native SwiftUI versions of the five browser card games under `thecardplace/`,
played against the computer, entirely offline. A hub opens the app; pick a
game, set your name, deal.

Nothing here wraps the web pages. The rules, the computer players and the
words the games say are ported from each game's `game.js` / `ai.js` / `ui.js`
into a Swift package, and the screens are SwiftUI on top of that.

## Building

Requires Xcode 26 and [xcodegen](https://github.com/yonaskolb/XcodeGen).

```bash
cd thecardplace/ios
xcodegen generate
open TheCardPlace.xcodeproj
```

The project file is generated from `project.yml`; regenerate it after adding
or removing source files. There are no other dependencies and no network
access anywhere in the app.

### Running it on a Mac

On an Apple Silicon Mac the iPad app runs natively: in Xcode choose the
**My Mac (Designed for iPad)** destination and run. From the command line the
same build is

```bash
xcodebuild -project TheCardPlace.xcodeproj -scheme TheCardPlace \
  -destination 'id=<your Mac's device id from xcodebuild -showdestinations>' \
  -allowProvisioningUpdates build
```

which leaves an iOS bundle under `Debug-iphoneos`. Finder cannot open that
directly; wrap it the way the App Store does and it launches:

```bash
mkdir -p "The Card Place.app/Wrapper" && cp -R ".../Debug-iphoneos/The Card Place.app" "The Card Place.app/Wrapper/" \
  && ln -s "Wrapper/The Card Place.app" "The Card Place.app/WrappedBundle" && open "The Card Place.app"
```

## Layout

```
project.yml                 the Xcode project, generated by xcodegen
CardGamesKit/               a Swift package: the games, with no UI in it
  ENGINES.md                the contract every engine meets
  Sources/CardCore          Card, Suit, Rank, seeded randomness, events, pace
  Sources/<Game>Engine      rules, computer players, review text, help text
  Tests/<Game>EngineTests   rules oracles, invariants, refusals, hidden information
TheCardPlace/               the app
  Hub/                      the opening screen and the per-game setup screen
  Shared/                   the announcement queue, the pace gate, settings,
                            cards, hand, trick, tables, log, help
  Games/<Game>/             one session (drives the engine) and one table screen
TheCardPlaceUITests/        plays each game from the accessibility labels alone
```

## Tests

The engines test on a Mac without a simulator:

```bash
cd thecardplace/ios/CardGamesKit && swift test
```

Each engine carries the same five suites its browser original has: a **rules
oracle** written as literal data (the trump order typed out card by card, the
scoring table as rows) and checked against the engine without calling the
functions on trial; **invariants** over hundreds of seeded hands played by the
computer on every difficulty; **refusals** with the state compared before and
after; **hidden information**, where every other seat's cards are scrambled and
the computer's decision must not change; and exact-string checks on the review
sentences.

The UI tests drive the real app on a simulator the way VoiceOver does — by the
labels on the controls, never by coordinates — and play a whole hand of each
game:

```bash
cd thecardplace/ios
xcodebuild -project TheCardPlace.xcodeproj -scheme TheCardPlace \
  -destination 'platform=iOS Simulator,name=iPhone 17' test
```

## Accessibility

The games are built so that nothing is conveyed by sight alone. The decisions
that matter, and where they live:

**Every card is a button with a full label.** "Jack of Clubs, left bower,
second highest trump, counts as spades, card 2 of 5." The label comes from the
engine's `describe` function, the same text the log uses. A card that may not
be played right now stays in the list and says why — "cannot be played, you
must follow hearts" — rather than being disabled, because a disabled control
is one a VoiceOver user may never learn exists. (`Shared/CardViews.swift`)

**Announcements are queued, one at a time.** VoiceOver will start a new
announcement on top of one it is half way through, so every message goes
through `Shared/Announcer.swift`, which posts the next only when VoiceOver
reports the last one finished. Something the player asks to hear jumps the
queue, and the game event it interrupted is put back rather than lost. Errors
interrupt. The most recent message is also shown as text with a Repeat button;
that text is not itself a live region, so nothing is spoken twice.

**Headings, not scrolling.** Every part of a table — What you can do, Your
hand, This trick, Last completed trick, the scores, What has happened — is a
heading the rotor can jump to. The hand is a group, so it can be skipped in
one move. (`Shared/TableViews.swift`)

**Focus follows the turn, unless told not to.** When it becomes the player's
turn VoiceOver moves to the first card they can play. Settings turns that off;
the announcement still says it is their turn.

**Pace in real seconds.** Immediate, Brisk, Comfortable, Relaxed, or Wait for
me — the same five rungs as the web, and the same defaults per game. Timed
paces are a ceiling: Continue is on screen during every pause and moves on at
once. (`Shared/PaceGate.swift`)

**Numbers belong to players.** Score tables are read row by row — "Ruth: score
12, tricks 3" — and partnership games score by side, not by a number repeated
in two columns.

**Nothing by colour alone.** Red suits are red, but the suit is printed as a
symbol, trump and points are written on the card, an unplayable card is greyed
and marked with a cross, a selected card has a thick border and a tick. The
red is 6.6:1 on white. Cards follow Dynamic Type up to the accessibility sizes
and wrap onto more rows rather than shrinking; every control is at least 44
points tall.

**A hardware keyboard works.** The review letters from the browser games —
H, T, L, S, C, O, R, N and each game's own — are keyboard shortcuts, and every
one is also a button in the Review menu.

### WCAG 2.2, briefly

| Criterion | How |
| --- | --- |
| 1.1.1 Non-text content | Card faces are hidden from VoiceOver; the button label carries everything the face shows. Icons are decorative or labelled. |
| 1.3.1 Info and relationships | Headings carry the header trait; tables are read as rows; the hand is a labelled group. |
| 1.3.3 Sensory characteristics | No instruction depends on position or colour. |
| 1.4.1 Use of colour | Suit symbols, text badges, cross and tick marks alongside colour. |
| 1.4.3 / 1.4.11 Contrast | Card ink 6.6:1 or better on its ground; system colours elsewhere; Increase Contrast thickens borders. |
| 1.4.4 / 1.4.12 Resize text | Dynamic Type everywhere, including inside cards via `@ScaledMetric`. |
| 1.4.10 Reflow | Cards wrap; nothing scrolls sideways. |
| 2.1.1 Keyboard | Every action is a button; shortcuts exist for review and Continue. |
| 2.2.1 Timing adjustable | Pace is a setting, and Wait for me removes the timer entirely. |
| 2.3.3 Animation from interactions | No animation is used for meaning; Reduce Motion is respected. |
| 2.4.3 Focus order | Reading order matches the screen: status, actions, hand, table, scores, log. |
| 2.4.6 Headings and labels | Every section is headed; every control is named. |
| 2.5.5 / 2.5.8 Target size | Every control is at least 44×44 points. |
| 3.2.2 On input | Choosing a card never submits anything on its own where a confirmation step exists (passing, burying, throwing to the crib). |
| 3.3.1 Error identification | A refusal says which rule stopped it, in the rule's words. |
| 4.1.2 Name, role, value | Native SwiftUI controls; selected cards carry the selected trait. |
| 4.1.3 Status messages | Every change is announced through the queue; the status line is text. |

## What is not here yet

- Playing with other people. The engines keep the same seat-checked
  `applyAction` gate the web games use so that a later version can add it,
  but this version is the computer opponents only.
- Difficulty in hearts and spades is carried but has no effect, because the
  browser originals' computer players never read it; euchre, cribbage and
  sheephead do differ by difficulty.
- Exporting the log and the bug reporter.
